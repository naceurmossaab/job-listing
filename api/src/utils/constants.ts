export enum Role {
  ADMIN = 'admin',
  EMPLOYER = 'employer',
  JOBSEEKER = 'jobseeker'
}

export enum JobStatus {
  PENDING = 'pending',
  REVIEWED = 'reviewed',
  REJECTED = 'rejected',
  HIRED = 'hired'
}

export enum Services {
  AUTH = "auth",
  USERS = "users",
  JOBS = "jobs",
}