export enum Role {
  ADMIN = 'admin',
  EMPLOYER = 'employer',
  JOBSEEKER = 'jobseeker'
}

export enum SubmissionStatus {
  PENDING = 'Pending',
  REVIEWED = 'Reviewed',
  HIRED = 'Hired',
  REJECTED = 'Rejected',
}

export enum Services {
  AUTH = "auth",
  USERS = "users",
  JOBS = "jobs",
  SUBMISSIONS = "submissions",
}